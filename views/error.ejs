<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container">
        <div class="row justify-content-center align-items-center min-vh-100">
            <div class="col-md-8">
                <div class="card shadow">
                    <div class="card-body p-5 text-center">
                        <i class="fas fa-exclamation-circle fa-5x text-danger mb-4"></i>
                        <h1 class="display-4 text-muted">Oops!</h1>
                        <h3 class="mb-3">Something went wrong</h3>
                        <p class="text-muted mb-4">
                            <%= message %>
                        </p>
                        
                        <% if (error && error.stack) { %>
                            <div class="alert alert-warning text-start mt-4">
                                <h6>Error Details:</h6>
                                <pre class="mb-0 small"><%= error.stack %></pre>
                            </div>
                        <% } %>
                        
                        <div class="mt-4">
                            <% if (user) { %>
                                <a href="/dashboard" class="btn btn-primary me-2">
                                    <i class="fas fa-home"></i> Go to Dashboard
                                </a>
                                <button onclick="history.back()" class="btn btn-outline-secondary me-2">
                                    <i class="fas fa-arrow-left"></i> Go Back
                                </button>
                                <a href="/auth/logout" class="btn btn-outline-danger">
                                    <i class="fas fa-sign-out-alt"></i> Logout
                                </a>
                            <% } else { %>
                                <a href="/auth/login" class="btn btn-primary me-2">
                                    <i class="fas fa-sign-in-alt"></i> Login
                                </a>
                                <button onclick="history.back()" class="btn btn-outline-secondary">
                                    <i class="fas fa-arrow-left"></i> Go Back
                                </button>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <script>
        // Auto-refresh in development mode after 5 seconds
        <% if (process.env.NODE_ENV === 'development') { %>
            setTimeout(() => {
                const refreshBtn = document.createElement('button');
                refreshBtn.className = 'btn btn-info btn-sm mt-3';
                refreshBtn.innerHTML = '<i class="fas fa-sync"></i> Auto-refresh in 5s';
                document.querySelector('.card-body').appendChild(refreshBtn);
                
                setTimeout(() => {
                    location.reload();
                }, 5000);
            }, 1000);
        <% } %>
    </script>
</body>
</html> -->